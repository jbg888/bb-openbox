{
  "name": "bb-openbox",
  "version": "1.0.0",
  "private": true,
  "description": "Best Buy Open-Box deal scraper + static deals page",
  "scripts": {
    "scrape": "node scrape.js",
    "probe": "node scrape.js --probe",
    "build": "node build.js"
  },
  "dependencies": {
    "playwright": "^1.50.0"
  }
}
